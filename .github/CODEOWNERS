# Who owns what.
#
# GitHub's permissions are repo-wide — there is no per-folder write access — and
# a CODEOWNERS entry only functions for someone who already has write access.
# Naming an outside contributor here would render as an error in the UI and
# imply a review gate that does not exist, so ownership below is only claimed
# where it is real.

*  @whiteoakmedia

# By convention, not by enforcement: the Windows track belongs to @jpeters0 —
# WINDOWS.md, scripts/build-windows.ps1, src-tauri/tauri.windows.conf.json,
# .github/workflows/windows.yml, and the #[cfg(windows)] blocks inside
# keepalive.rs, ndi.rs and diag.rs. He wrote them and has Windows booth
# hardware to validate them on, which this booth does not. Changes there should
# go past him first.
